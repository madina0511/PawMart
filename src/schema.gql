# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AiResponse {
  answer: String!
  sessionId: ID!
}

input AskAiInput {
  petId: ID
  question: String!
  sessionId: ID
}

type AuthType {
  accessToken: String!
  refreshToken: String!
  user: UserType!
}

input CancelPaymentInput {
  cancelReason: String!
  paymentKey: String!
}

type ChatMessage {
  content: String!
  role: String!
  timestamp: DateTime!
}

type ChatSession {
  _id: ID!
  messages: [ChatMessage!]!
  petId: ID
}

input ConfirmPaymentInput {
  amount: Float!
  orderId: ID!
  orderId_toss: String!
  paymentKey: String!
}

input CreateOrderInput {
  items: [OrderItemInput!]!
  shippingAddress: ShippingAddressInput!
}

input CreatePetInput {
  age: Float
  breed: String
  color: String
  description: String
  images: [String!]! = []
  name: String!
  neutered: Boolean
  price: Float!
  species: PetSpecies!
  status: PetStatus! = AVAILABLE
  vaccinated: Boolean
  weight: Float
}

input CreateProductInput {
  brand: String
  category: ProductCategory!
  description: String!
  images: [String!]
  name: String!
  petType: [PetType!]!
  price: Float!
  stock: Int!
  tags: [String!]
}

input CreateReviewInput {
  comment: String!
  images: [String!]
  productId: ID!
  rating: Int!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  askAI(askAiInput: AskAiInput!): AiResponse!
  cancelPayment(cancelPaymentInput: CancelPaymentInput!): Payment!
  confirmPayment(confirmPaymentInput: ConfirmPaymentInput!): Payment!
  createOrder(input: CreateOrderInput!): OrderType!
  createPet(input: CreatePetInput!): Pet!
  createProduct(input: CreateProductInput!): ProductType!
  createReview(createReviewInput: CreateReviewInput!): Review!
  deletePet(id: ID!): Boolean!
  deleteProduct(id: ID!): Boolean!
  deleteReview(id: ID!): Boolean!
  login(input: LoginInput!): AuthType!
  refreshTokens(refreshToken: String!): AuthType!
  register(input: RegisterInput!): AuthType!
  updateOrderStatus(input: UpdateOrderInput!): OrderType!
  updatePet(id: ID!, input: UpdatePetInput!): Pet!
  updateProduct(input: UpdateProductInput!): ProductType!
  updateReview(id: ID!, updateReviewInput: UpdateReviewInput!): Review!
}

input OrderItemInput {
  productId: ID!
  quantity: Int!
}

type OrderItemType {
  price: Float!
  productId: ID!
  quantity: Int!
}

enum OrderStatus {
  CANCELLED
  DELIVERED
  PAID
  PENDING
  SHIPPING
}

type OrderType {
  _id: ID!
  items: [OrderItemType!]!
  paymentId: String
  shippingAddress: ShippingAddressType!
  status: OrderStatus!
  totalAmount: Float!
  userId: ID!
}

type Payment {
  _id: ID!
  amount: Float!
  failReason: String
  method: String
  orderId: ID!
  orderId_toss: String!
  paymentKey: String!
  status: String!
  userId: ID!
}

type Pet {
  _id: ID!
  age: Float
  breed: String
  color: String
  description: String
  images: [String!]!
  name: String!
  neutered: Boolean
  price: Float!
  species: PetSpecies!
  status: PetStatus!
  vaccinated: Boolean
  weight: Float
}

enum PetSpecies {
  BIRD
  CAT
  DOG
  FISH
  OTHER
  RABBIT
}

enum PetStatus {
  AVAILABLE
  RESERVED
  SOLD
}

enum PetType {
  BIRD
  CAT
  DOG
  FISH
  OTHER
}

enum ProductCategory {
  ACCESSORY
  CARE
  FOOD
  MEDICINE
  TOY
}

type ProductType {
  _id: ID!
  brand: String
  category: ProductCategory!
  description: String!
  images: [String!]!
  name: String!
  petType: [PetType!]!
  price: Float!
  rating: Float!
  reviewCount: Int!
  stock: Int!
  tags: [String!]!
}

type Query {
  allOrders: [OrderType!]!
  chatHistory(sessionId: ID!): ChatSession!
  hello: String!
  myOrders: [OrderType!]!
  myReviews: [Review!]!
  mySessions: [ChatSession!]!
  order(id: ID!): OrderType!
  paymentByOrder(orderId: ID!): Payment
  pet(id: ID!): Pet!
  pets: [Pet!]!
  product(id: ID!): ProductType!
  products: [ProductType!]!
  productsByCategory(category: String!): [ProductType!]!
  productsByPetType(petType: String!): [ProductType!]!
  reviewsByProduct(productId: ID!): [Review!]!
  searchProducts(query: String!): [ProductType!]!
}

input RegisterInput {
  email: String!
  name: String!
  password: String!
}

type Review {
  _id: ID!
  comment: String!
  images: [String!]!
  productId: ID!
  rating: Int!
  userId: ID!
}

input ShippingAddressInput {
  address: String!
  name: String!
  phone: String!
}

type ShippingAddressType {
  address: String!
  name: String!
  phone: String!
}

input UpdateOrderInput {
  _id: ID!
  status: OrderStatus!
}

input UpdatePetInput {
  age: Float
  breed: String
  color: String
  description: String
  images: [String!]
  name: String
  neutered: Boolean
  price: Float
  species: PetSpecies
  status: PetStatus
  vaccinated: Boolean
  weight: Float
}

input UpdateProductInput {
  _id: ID!
  brand: String
  category: ProductCategory
  description: String
  images: [String!]
  name: String
  petType: [PetType!]
  price: Float
  stock: Int
  tags: [String!]
}

input UpdateReviewInput {
  comment: String
  images: [String!]
  rating: Int
}

enum UserRole {
  ADMIN
  USER
}

type UserType {
  _id: ID!
  avatar: String
  email: String!
  name: String!
  role: UserRole!
}